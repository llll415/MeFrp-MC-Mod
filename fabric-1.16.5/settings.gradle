pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'mefrp-mc-mod-fabric-1.16.5'

include 'common-legacy'
project(':common-legacy').projectDir = file('../common-legacy')
